// DEFAULT for the @modal slot. When the URL doesn't address the modal (e.g. on
// /gallery itself, or a hard load of /gallery/photo/[id]), the slot renders
// nothing.

export default function ModalDefault() {
  return null;
}
